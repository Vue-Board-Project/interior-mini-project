<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
                 
<mapper namespace="com.mycompany.webapp.dao.mybatis.UsersDao">
	<insert id="insert" parameterType="users">
		INSERT INTO users
		(email, password, name, phone, postcode, address, address_detail, crn, role)
		VALUES (#{email}, #{password}, #{name}, #{phone}, #{postcode}, #{address}, #{addressDetail}, #{crn}, 'USER')
	</insert>
	
	<select id="selectByEmail" parameterType="string" resultType="users">
		SELECT email, password, name, phone, postcode, address, address_detail, crn, role
		FROM users
		WHERE email = #{email}
	</select>
</mapper>